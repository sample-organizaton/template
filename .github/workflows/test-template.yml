name: Test workflow template

on:
  push:
    branches: [ "main" ]

jobs:
  echo:
    runs-on: self-hosted

    steps:
      - name: Run a one-line script
        run: echo "Hello this is a workflow template"
